# Project details — Built with, Try it out links, Project Media

Form: **Project details → Built with / "Try it out" links / Project Media**

---

## Built with
*(up to 25 tags — Devpost tags are freeform text, lowercase-with-hyphens is
the usual convention)*

Paste these in one at a time (Devpost auto-suggests as you type; pick the
matching suggestion where one exists, otherwise it adds it as a new tag):

```
gemini
google-adk
google-cloud
cloud-run
firestore
gemma
veo
lyria
python
fastapi
react
typescript
vite
tailwindcss
zustand
build123d
model-context-protocol
docker
nodejs
youtube-data-api
google-search
ollama
code-server
pydantic
bambu-studio
```

That's 24 — one slot free. (Dropped the duplicate `mcp`/`model-context-protocol`
pair down to just `model-context-protocol`, and used the freed slot for
`bambu-studio` since the printer integration is a real, demoable feature.)

**Action for you:** paste these into the tag field.

---

## "Try it out" links
*(links where people can try the project or see the code)*

| Link | URL |
|---|---|
| Hosted project | `<-- fill in once deployed to Cloud Run + frontend host, e.g. Firebase Hosting -->` |
| GitHub repo | `https://github.com/Its-a-me-Ashwin/Anvil` |

**Action for you:**
- [ ] Deploy the frontend somewhere public (Firebase Hosting / Cloud
      Storage + Load Balancer, per `deploy.md`) and paste the URL above.
- [ ] Confirm the repo is public, or that `testing@devpost.com` and
      `cloudhackathons@google.com` have access if it stays private.
- [ ] Add both links in the form.

---

## Project Media — Image gallery
*(JPG/PNG/GIF, 5 MB max, 3:2 ratio recommended, up to 15 images)*

See `assets/screenshots/` in this folder — action items for exactly which
screens to capture are in `CHECKLIST.md`. Once captured, drag them into
the gallery upload box in this order (first image becomes the cover):

1. `thumbnail.png` (the cover / hero shot — see `assets/thumbnail.png`)
2. `architecture-diagram-tools.png` — only if it didn't fit in the
   Additional Info upload slot above; skip if it's already there
3. Three-pane workspace mid-conversation (tool-call cards visible)
4. CAD viewport with a generated assembly
5. Wiring diagram
6. Memory tab — skill radar chart
7. Printer camera tab with a Gemma alert banner
8. Cloud Run console showing the live service (the "proof" shot)

---

## Project Media — Video demo link
*(embeds at the top of the public project page)*

```
<-- paste the YouTube/Vimeo URL here once the video is uploaded -->
```

**Action for you:** this is the same video used for the mandatory "Demo
video" field later in the form — one upload, two places it's linked.
